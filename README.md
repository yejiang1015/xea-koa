# @xea/koa-core

## node 模板 快速测试

```json
"nodemonConfig": {
    "ignore": [
      "node_modules/",
      "build/",
      "dist/"
    ],
    "ext": "ts"
  },

```

## packages

```json
"nodemonConfig": {
    "ignore": [
      "node_modules/",
      "build/",
      "dist/"
    ],
    "ext": "ts"
  },

```

## use

```js
import { app, route } from "@xea/koa";

route.get("/", (ctx: Koa.Context) => {});
instance.listen(8080, () => {});
```
